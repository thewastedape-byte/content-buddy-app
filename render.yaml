services:
  - type: web
    name: content-buddy-app
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: NEXT_PUBLIC_APP_URL
        value: https://contentbuddy.thewastedape.com
